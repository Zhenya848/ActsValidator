syntax = "proto3";

option csharp_namespace = "UserService.Presentation";

package greet;

service Greeter {
  rpc MakeAction (MakeActionRequest) returns (MakeActionResponse);
}

message MakeActionRequest {
  string user_id = 1;
}

message MakeActionResponse {
  
}